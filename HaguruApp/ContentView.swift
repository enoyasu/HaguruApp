import SwiftUI

// ContentView は HaguruApp では使用しません。
// エントリポイントは HaguruApp/App/HaguruAppApp.swift の RootView です。
// このファイルは互換性のために残しています。

struct ContentView: View {
    var body: some View {
        RootView()
    }
}
